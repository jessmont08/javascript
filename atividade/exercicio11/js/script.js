const base = prompt("Insira a base: ")
const altura = prompt("Insira a altura: ")

const triangulo= Number(base) * Number(altura)
const resultado= Number(retangulo) / 2

console.log("O aréa é" + resultado)