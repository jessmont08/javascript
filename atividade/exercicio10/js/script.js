const base = prompt("Insira a base: ")
const altura = prompt("Insira a altura: ")

const retangulo= Number(base) + Number(altura)
const resultado= 2 * Number(retangulo)

console.log("O perímetro é" + resultado)