const a = prompt("Insira a primeira nota:");
const b = prompt("Insira a segunda nota:");
const c = prompt("Insira a terceira nota:");
const d = prompt("Insira a quarta a nota:");

const resultado = Number(a) + Number(b) + Number(c) + Number(d);
const media = resultado / 4

console.log(media);
